import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Logged in ✅").font(.title2)
            Text("This is ContentView").foregroundColor(.secondary)
        }
        .padding()
    }
}

